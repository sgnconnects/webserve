package pl.kmolski.webserve.http;

public enum HttpRequestMethod {
    GET,
    HEAD,
    POST,
    PUT,
    DELETE,
    CONNECT, // not supported
    OPTIONS,
    TRACE,
    PATCH // not supported
}
